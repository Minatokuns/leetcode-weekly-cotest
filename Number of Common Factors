```
//EASY LC
class Solution {
    public int commonFactors(int a, int b) {
        int min = Math.min(a,b); // max common no. can be the minimum of the 2 no.
        int count = 1; // as they will always be dvide be 1
            for(int i = 2; i<=min;i++){
                //if they are both divisible by the no. count increases 
                if(a%i==0 && b%i==0){
                    count++;
                }
            }
        return count;
    }
}
```
